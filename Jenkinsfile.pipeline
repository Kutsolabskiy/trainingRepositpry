pipeline {
    agent any
    tools {
        maven "Maven3"
    }

    stages {
        stage('Build') {
            steps {
                bat "cd $WORKSPACE"
                bat "dir"
                bat "mvn clean test"
            }
        }
    }
}